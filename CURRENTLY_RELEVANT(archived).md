# Currently relevant — state of play

*A living snapshot: what is built, what is next, what is blocked and on whom. Unlike
`REPORT.md` and `REPORT_PLAIN_ENGLISH.md` (append-only chronological records), this file is
overwritten to reflect the present. When something here changes, edit it in place.*

**Last updated:** 2026-08-15 · **Last major event:** both overlaps now modelled and on a level
footing. Sum peak biases Bi M by ~100% at low x; overlap (i) costs Bi L at most ~13%. **Bi Lα /
As Kα survives as the recommended route.**

---

## ▶ Cold start

*Read in this order:*

1. **`CLAUDE.md`** — governance (physics values are never invented; the run loop stores raw
   only) and the trap list. Non-negotiable.
2. **This file** — the roadmap below is the work queue. Start at the top of Phase 0/1.
3. **`REPORT.md` §18** — the k\* table, the "A" resolution, the literature cross-check, and
   §18.5 on why the K/L re-indexing mattered.

**State to rely on:** 250 tests green, golden gate clean, 21 runs in `Sim\Results\` (15 frozen + Set C),
17 analysis-ready outputs in `Sim\Aggregated\`, 2 figures in `Sim\Figures\`.
Nothing is in flight. **`NEXT_SESSION_PROMPT.md` holds the cold-start prompt.**

---

## Where the build is

| Stage | State |
|---|---|
| 1. Input generator | **Done**, golden-gated (`inputgen.py`, `spec.py`). |
| 2. Output parser | **Done**, golden-gated (`parser.py`). |
| 3. Executor | **Done 2026-08-13** — `executor.py`, rebuilt after the original scratchpad script was found missing. Single run + serial matrix, crash vs silent-sim-failure distinguished, **refuses to overwrite existing outputs**. Validated on the real binary (quarantined run_id, 19 files, parsed clean, artifacts removed). REPORT §22. |
| 4. Matrix generator | **Done** — `matrix.py`. `production_matrix()` stays frozen at 15 (Set A 10 + Set B 5) so pre-2026-08-13 results remain reproducible; `extension_matrix()` adds Set C (6), `full_matrix()` is all 21. |
| 5. Aggregator + manifest | **Done** — `aggregate.py`. |
| 6a. Diagnostic ratios | **Done** — `ratios.py`, both line definitions. `SHELLS[BI]` now carries K, L, M. |
| 6b. Sum-peak synthesis | **Done 2026-08-15** — `sumpeak.py`. Four modelling choices as switches; `as_measured_unknowns()` corrupts the unknown only (corrupting both sides self-cancels). REPORT §26. |
| 6c. Overlap (i) bound | **Done 2026-08-15** — `overlap.py`. **The two overlaps are different problems:** sum peak 0.47 FWHM (merged), overlap (i) 1.74 FWHM (resolvable). Bounded as fitted-area uncertainty, swept. Recommendation survives. REPORT §27. |
| 7a. k\* calibration | **Done** — `kfactors.py`. Set A curves, Set B curves and the invariance summary all persisted. |
| 7b. K/L re-indexing | **Done** — `kl_index.py`. Replicates Walther Figs 3 (bottom), 4, 5. |
| 7c. Held-out round trip | **Done 2026-08-15** — `recover_x_via_kl()` is held out in *x* AND *t*, indexed on measured As K/L. Thickness-indexed version kept for comparison. **Held-out is slightly BETTER (0.00342 vs 0.00377)** — Walther's self-calibration claim, demonstrated. REPORT §25. |
| 8. Figures | **Started 2026-08-14** — `figures/` package, 2 of ~8 planned. Presentation layer only; `mcxray_wrapper/` imports no matplotlib. |
| 7d. Inversion verdict | **Ethan's.** Must be rendered on *corrupted* data, not the clean round trip. |

**Analysis-ready outputs** in `C:\MCXRAY\Sim\Aggregated\`:
`combined_intensities.csv` (441 rows = 21 runs × 21 lines, raw), `provenance_manifest.json`,
`diagnostic_ratios.csv` (21 rows), `kstar_calibration_setA.csv` (320 rows),
`kstar_vs_kl_ratio_setA.csv` (240 rows), `kstar_composition_setB.csv` (120 rows),
`kstar_invariance_setB.csv` (24 rows), `roundtrip_recovered_x.csv`, `roundtrip_summary.csv`,
`corner_rho_t_setA_vs_setC.csv` (84 rows), `roundtrip_heldout_as_k_l.csv`,
`roundtrip_heldout_ga_k_l.csv`, `sumpeak_sensitivity.csv`, `overlap_i_bound.csv`.

**Figures** in `C:\MCXRAY\Sim\Figures\` (PNG 200 dpi + vector PDF): `fig01_ratios_vs_thickness`
(replicates Walther Fig. 1), `fig02_rho_t_insufficiency` (the Set C finding). Rendered by the
`figures/` package — matplotlib lives there and nowhere else. `python -m figures.make_all`.

**Published report:** `kstar_calibration_setA_report.html` (repo snapshot), regenerated by
`make_standalone.py` from the page source in `artifact/kstar-calibration.html` — both moved
into the repo 2026-08-17, having been the last things still living only in a scratchpad. ·
https://claude.ai/code/artifact/24626073-4341-4a38-9b72-88db52be13f2 (master).

---

## ▶ ROADMAP — practical work remaining

*Ordered by dependency. Phases 0–2 need nothing from Walther. Phase 3 is the gate everything
downstream waits behind. Sizes are rough build effort, excluding review.*

### Phase 0 — housekeeping (unblocked)

| # | Task | Size |
|---|---|---|
| ~~**P0.1**~~ | ~~**Persist the Set B invariance check.**~~ **DONE 2026-08-13.** Added `composition_curves()` as the Set B mirror of `calibration_curves()`, plus `write_composition_curves` / `write_invariance` → `kstar_composition_setB.csv` (120 rows) + `kstar_invariance_setB.csv` (24 rows). Two files because the summary can't tell a systematic drift from MC scatter. **Result: all 24 curves strictly monotonic in x — zero scatter**, and `Bi_As L/K principal` drifts 0.0062%, six times *below* the 0.037% MC noise floor, i.e. invariant to the limit of resolution. REPORT.md §19. | ✓ |
| ~~**P0.2**~~ | ~~**`__init__.py` exports are stale.**~~ **DONE 2026-08-13.** Now exports the derive half (`compute_k_star`, `calibration_curves`, `composition_curves`, `invariance_check`, `index_on_kl`, `recover_x`, `summarize`) and the docstring describes both halves and the raw-only rule. | ✓ |

### Phase 1 — close LO3, strengthen the round trip (unblocked)

| # | Task | Size |
|---|---|---|
| ~~**P1.1**~~ | ~~**Bi K lines into the analysis.**~~ **DONE 2026-08-13.** `SHELLS[BI]` gained `K`; Bi now has three heavy shells — 16 pairs per run, not 12. All derived tables regenerated (calibration 320 rows, K/L index 320, Set B 160, invariance 32, round trip 160). **The result inverts the expectation:** in the simulation Bi K is the *best-behaved* route (invariance 0.0009, recovery error 0.00177 vs 0.00496 for everything else) because Bi Kα at 77 keV is barely absorbed. It is unusable anyway — **I(Bi Lα)/I(Bi Kα1) = 103**, dose-independent, so equal statistics needs ~10⁴× the dose. **Bi K fails on counting statistics, not absorption.** REPORT.md §20. | ✓ |
| ~~**P1.2**~~ | ~~**Re-index the round trip on K/L.**~~ **DONE 2026-08-15.** `recover_x_via_kl()`, As K/L axis (evidence-based, §23.5). Interpolation error measured on a held-out point: 0.17% mean. **Held-out recovery beats handed-thickness recovery** (0.00342 vs 0.00377) — self-calibration confirmed. **As K/L beats Ga K/L by 47%**, independently confirming Set C. REPORT §25. | ✓ |

| **P1.3** | **Model counting statistics.** *(Added 2026-08-13, surfaced by P1.1.)* The simulation reports **expectation values**, and the k\* algebra closes on them however few photons underlie the number — which is why 0.4 detected Bi Kα photons still yield an excellent recovered *x*. **Nothing in the pipeline models Poisson noise, anywhere.** Absorption is modelled and the overlaps will be; statistics is not. This matters far beyond Bi K: at x = 0.01 / 100 nm the stored Bi Lα is 116 photons (9.3%) and Bi Mα is 76 (11.4%), and those propagate straight into recovered *x*. **Counting statistics may dominate the low-x failure mode ahead of either overlap**, which would make this more decisive for LO4 than the sum peak. Standard Poisson propagation on already-stored intensities — needs nothing from Walther. **Reference dose now settled:** 1 nA × **715.5 s live** (Walther, 2026-08-13) — scale factor **7.155** on the stored 100 s basis, linearity verified, no re-runs. His current is an estimate not a measurement, so still sweep dose with that as the labelled reference point. | ~3 hr |

### Phase 2 — corner infill (gated on Ethan, not Walther)

| # | Task | Size |
|---|---|---|
| ~~**P2.1**~~ | ~~**Stage 3 executor as a proper module.**~~ **DONE 2026-08-13.** Rebuilt from scratch (`executor.py`, 15 tests). Smoke-tested against the real simulator: 5.6 s, 19 outputs, 21 parsed rows, zero production files touched. Reproducibility hole closed. REPORT §22. | ✓ |
| ~~**P2.2**~~ | ~~**Fill the untested corner.**~~ **DONE 2026-08-13.** Set C: 6 runs, x = 0.01, 32→1024 nm, 25.4 min, all clean. **Result: ρ·t is NOT a sufficient absorption coordinate.** Set A mispredicts Set C by **−22.8%** at 1024 nm (matched ρ·t); confirmed by log-log interpolation and by an assumption-free same-thickness check (−1.5% → −26.3%). **And the effect is line-dependent: Ga K/L carries a 22.8% composition residual beyond density, As K/L only ~2%.** → index P1.2 on **As K/L**. REPORT §23. | ✓ |

### Phase 3 — the corruption models (GATED ON WALTHER)

*Walther replied 2026-08-13. The gate is **partly open**: the application point is decided and
the exposure is settled. What remains is a modelling choice this project must make and state,
not something to wait on. See "Answered by Walther" below.*

| # | Task | Size |
|---|---|---|
| **P3.1** | **Sum-peak synthesis** (Stage 6b). Analytic Ga Lα + As Lα pile-up added onto stored Bi Mα, emitting a corrupted Bi L/M ratio at 0.1% / 1% / 10% beside the clean one. Reads the stored table only; never re-runs the simulator; computes no k-factors. Mirror `ratios.py`; test hermetically like `tests/test_ratios.py`. | ~4 hr once unblocked |
| **P3.2** | **Overlap (i) model** — As Kα1 (10.543 keV) vs Bi Lα (10.840 keV), 297 eV apart, with Ga Kβ1/Kβ2 (10.263 / 10.365) in the same window. **Currently unscoped entirely.** It lands on `Bi_As L/K`, the best-behaved route in the whole matrix, so modelling only the sum peak means modelling the less consequential of the two overlaps. | Unknown — scope after Walther answers |

### Phase 4 — the conclusion (needs Phase 3)

| # | Task | Size |
|---|---|---|
| **P4.1** | **Corrupted round trip.** Recovered *x* vs true *x* at each corruption level and each overlap treatment, using the P1.2 K/L-indexed recovery. | ~2 hr |
| **P4.2** | **Measurability limit / error budget.** At what *x* does recovery error exceed a stated threshold, and which mechanism dominates there. This is LO4's actual deliverable. | ~3 hr |
| **P4.3** | **The verdict.** **Ethan's. Not automated.** | — |

### Optional, parallel, unblocked

| # | Task | Size |
|---|---|---|
| **PX.1** | **Plot Fig. 1.** The three diagnostic ratios vs thickness are replicated in data but have never been plotted — the one replicated curve that can't be shown. | 20 min |
| **PX.2** | **Digitise Walther's Figs 3–5.** Extract his plotted points into a clearly-labelled external dataset (never merged into ours) for a true overlay. Would turn the Bi M offset from "~1.5×, eyeballed" into a measured per-route number. | ~2 hr |
| **PX.3** | **Decide the fate of the 30 obsolete comparison runs** (`_wal_`/`_tix_`, wrong density + detector) in `Results\` — clear, or keep as on-disk evidence of the ρ·t divergence? ~570 files in a flat directory. Ethan's call. | — |

### Critical path

```
P0.1 ✓  P0.2 ✓  P1.1 ✓  P2.1 ✓  P2.2 ✓        Walther answered 2026-08-13:
                                               application point decided, exposure settled
P1.2 ─┐
P1.3 ─┼──► P3.1 (sum peak) ──► P4.1 ──► P4.2 ──► P4.3 (Ethan's verdict)
      │    P3.2 (overlap i) ──┘
      └──  no external blocker remains
```

**Done 2026-08-13:** P0.1, P0.2, P1.1, P2.1, P2.2. **Remaining:** P1.2, P1.3, P3.1, P3.2 (if
scoped), P4.1, P4.2, P4.3.

**Walther is offline 16-23 Aug; meeting 26-28 Aug (2026-08-14). His instruction: "just complete
your simulations and prepare the plots." Nothing is blocked on him any more —** His reply settled where the sum-peak artefact is
applied (unknown only) and the reference exposure. The three remaining sum-peak choices are
**this project's to state and sweep** — he fitted his magnitude to close a gap, so there is no
method to inherit. P3.1 is buildable today.

**The schedule risk is P3.2 (overlap i)** — unscoped, and it lands on the best-behaved route in
the matrix. Decide by 2026-08-14: model it, bound it, or declare it a limitation.
See `WORKPLAN_17AUG.md` for the day-by-day plan to the 17 August target.

---

## Answered by Walther (2026-08-13) — what the reply settled

*Ethan sent the message 2026-08-13; Walther replied the same day. REPORT.md §21 is the full
record. Summary of what is now decided and what is not:*

**✓ Bi M factor PINNED to 1.506 ± 0.020** *(2026-08-14, from his Fig. 1)*. Four thicknesses,
flat across an 8× range — a fixed difference in I(Bi Mα), not an absorption effect. Same figure
shows **Ga K/L and As K/L agreeing within reading error across two different codes** (his Fig. 1
is CASINO). Also settles a side question: his ratios match our `principal` definition, not
`summed`. REPORT §21.1b.

**✓ Bi M discrepancy — CONFIRMED, and re-explained.** His simulated **Bi L/M ≈ 1:1**; ours is
**1.515** at 100 nm. **1.51×**, from a number he wrote in text — so the finding no longer rests
on reading his figures by eye, which is what it rested on before. Our Bi L routes match his
published constants to 3%, so the deduction is clean: **our Bi Mα is ~1.5× weaker than his**.
**The mechanism proposed in REPORT §20 was wrong** — both CASINO and MC X-Ray simulate Mα only,
so band coverage cannot explain it. His answer: *"the references may numerically differ"*, i.e.
a **code-to-code difference in the Bi Mα atomic data**. Right quantity, wrong mechanism.

**✓ "Are Bi M-lines beyond Mα modelled?" — CLOSED.** No. Confirmed both by him and by our own
output, which carries exactly one Bi M line (`Line Ma`).

**✓ The simulation → experiment M-band factor: ×1.743.** For simulated Mα1 = 100, the
unresolved band totals 174.3 (Mβ alone is 62.4, sitting 103 eV from Mα). **Not** the explanation
for the discrepancy above — a separate conversion, and load-bearing for Stage 6b because
**the sum peak lands on the experimental M peak, not the simulated Mα**.

**✓ Sum peak, where applied — DECIDED.** *"Only applied to the experimental spectrum, not to
simulations."* His calibration is simulated (clean), his unknown is measured (corrupted) — the
**calibrate-clean → invert-corrupted** variant, which does **not** self-cancel and so gives the
largest induced error. This was the fourth modelling decision, flagged as mattering more than
the other three.

**✓ His 600 counts was FITTED, not measured** — chosen so his measured Bi L/M (~2:3) would agree
with his simulation (~1:1). It is therefore entangled with *his* Bi M value, which differs from
ours by ~1.5×. **The 15% figure is not transferable here.** Vindicates sweeping 0.1 / 1 / 10%.

**✓ Exposure — SETTLED.** 1 nA (his estimate, not a measurement) × **715.5 s live**. Scale
factor **7.155** on the stored 100 s basis; linearity verified; no re-runs.

### Still ours to decide, not his to answer

The first three sum-peak questions are **not** waiting on him. His magnitude was obtained by
fitting to agreement rather than from a pile-up rate, so there is no published answer to
inherit:

1. **Exact lines** — Ga Lα + As Lα only, or include Lβ?
2. **What the fraction multiplies** — a fraction of the Ga Lα / As Lα *intensity*, or a true
   pile-up rate ∝ product of count rates?
3. **Photon conservation** — add the fake counts to Bi Mα only, or also deplete Ga Lα / As Lα?

These are modelling choices for this project to **state and sweep**, not questions to send.

### Worth raising in the next message

- **Our simulated Bi L/M is 1.515 against his 1.0.** He gave his number without knowing ours;
  telling him closes the loop and hands him concrete evidence for the atomic-data difference he
  suspected.
- **Clarify the run matrix.** The sent email described it as "2–1024 nm at x = 0.01–0.2 at
  100 nm", which merges the two sets. It is a **cross**: Set A = 10 runs, 2–1024 nm, all at
  x = 0.20; Set B = 5 runs, x = 0.01–0.20, all at 100 nm. He did not comment on the k\* table at
  all — possibly because that description made it hard to interpret.
- **Does overlap (i) need a model?** As Kα1 / Bi Lα, 297 eV. Never asked. It lands on the
  best-behaved route in the matrix and nothing in the plan touches it.

## Recently made clear (2026-08-13)

- **"A" is the ATOMIC WEIGHT.** ρ is the atomic density — a separate quantity that does **not**
  enter Eqs (2), (4), (9). (Walther, personal communication, August 2026.) `ATOMIC_WEIGHTS`
  (IUPAC/CIAAW 2024) was already correct; every k\* is **absolute**, not correct-up-to-a-factor.
  **No stored number changed value.** REPORT.md §18.2.
- **All three of his published constants are reproduced** from our data — k\*<sub>BiL,AsK</sub>
  2.4161 vs 2.490 ± 0.071, k\*<sub>BiL,GaK</sub> 2.8466 vs 2.895 ± 0.034, k\*<sub>GaK,AsK</sub>
  0.8489 vs 0.861 ± 0.011. All 1.4–3.0% low, same sign — a small systematic (density, code),
  not an algebra fault. REPORT.md §18.3.
- **Shapes agree with his figures.** His captions say "all are exponential curves"; his own
  plots show the rising routes as straight lines, which is what our data gives. The caption is
  loose, not a claim his figures support.
- **`Bi Lα ÷ As Kα` is the standout route** — 0.7% thickness drift over 512×, 0.0001 relative
  spread across x = 0.01–0.20. **But it sits on overlap (i).** The best-behaved route is the one
  the detector cannot cleanly resolve. That tension is the finding.

## Recently made clear (2026-07-19 meeting)

- **Density model fixed:** ρ(x) = 5.32 + 1.86x (`WALTHER_REVISED`), the only production model.
- **Density precision barely matters** — k\* calibration is self-correcting against a density
  offset; it cancels in the calibrate-then-invert round trip. Now supported empirically: 6.2%
  denser than his runs, 1.4–3.0% apart in k\*.
- **Windowless is impossible** — confirmed, no workaround. Set A is 10 runs, not 20.
- **MC X-Ray is the correct tool, not a compromise.** CASINO can't do sub-lines or detector
  properties.
- **Detector:** Si:Li, 0.5 cm crystal, radius 0.3. Bi Kα1 efficiency 0.150 → 0.237.
- **Electron count sets simulation *precision*, not photon counts.** Thin foils need 10⁷ for
  <1% MC noise. (LO4 states 10⁶ — the deviation is deliberate and must be declared.)
- **Lα2 is not a concern** — Bi Lα1/Lα2 are ~108 eV apart, inside one resolution element, so
  the reported `La` is the whole bundle.
- **`RandomNumberSeed=0` is not deterministic** — byte-identical inputs differ by ≤0.037%.

## Still open

- **Secondary fluorescence** in MC X-Ray? (His own sims exclude it.) Never asked.
- **Why is our Bi Mα ~1.5× weaker than his?** Confirmed real (REPORT §21.1) and attributed to
  differing atomic-data references, but the specific source — cross-section, fluorescence yield
  or transition probability — is unidentified. Bounds any absolute claim resting on Bi M.
- **Does overlap (i) need a model?** As Kα1 / Bi Lα, 297 eV. Never asked; nothing in the plan
  touches it, and it lands on the best-behaved route in the matrix.
- **Exposure (`BeamCurrent` × `AcquisitionTime`) — SETTLED 2026-08-13.** These set
  the absolute photon counts and were inherited from golden (1 nA, 100 s). Ethan's decision:
  **match Walther's experiment.** What that can and cannot achieve:
  - **Acquisition time: matchable.** His Fig. 2 gives **743 s total, 3.7% dead time** → live
    time **715.5 s**. MC X-Ray has no dead-time concept, so its `AcquisitionTime` is live time.
  - **Beam current: ~1 nA, but his ESTIMATE, not a measurement** ("was not measured but will
    have been about 1 nA", 2026-08-13). Our runs already use 1 nA, so the current matches — but
    on an unquantified uncertainty.
  - **Therefore P1.3 still sweeps dose** rather than fixing it, with 1 nA × 715.5 s as the
    labelled reference point. His estimate becomes a marked point on an axis, not a hidden
    assumption.
  - **No re-runs.** Verified from the stored table: photon counts = (photons/e/sr) × dose ×
    solid angle (0.0176 sr) × detector efficiency, with the efficiency correlation exactly
    1.0000. Scaling is identical to re-simulating. **Raw stays raw at its own 100 s basis**;
    dose is applied in the derived layer with both values recorded — which also keeps every run,
    including any future P2.2 corner runs, on one common raw footing.
- **`.sim` path resolution + non-zero seed** — gates parallelism only (optional; 15 runs ≈
  50 min serial).

---

## Traps that stay true regardless (don't re-learn these)

- Lengths in the `.sam` are **Ångströms**, not nm. 100 nm → 1000.
- Composition is **weight** fraction, not atomic.
- `UserDefinedMassDensity=0` triggers a wrong auto-mix silently — always set it.
- `DetectorDiffusionLenght` misspelling is load-bearing. Never "correct" it.
- CRLF with trailing CRLF at EOF on the four Ethan-authored inputs; `.mdl`/`.rp` are LF-only;
  output CSVs are LF-only. Per-file fidelity — do not harmonise.
- Output CSV separator is `", "`; rows end in a trailing comma; key on **`Atomic number`**.
- Golden files are ground truth for **format**, never for **physics values**.
- `Golden/` is read-only.
- **"18 lines" is not a row count** — the intensity CSV has **21 rows** (Ga 6, As 6, Bi 9).
  Assert counts from the file, not prose.
